services:
  backend:
    build:
      context: ./calculator-back
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./calculator-web
      args:
        VITE_API_URL: /api
    ports:
      - "3000:80"
    depends_on:
      - backend
